import React from "react";

const Navbar = () => {
  return (
    <nav className="navbar bg-green-50 flex justify-between items-center px-6 py-4 rounded-full fixed top-4"></nav>
  );
};

export default Navbar;
